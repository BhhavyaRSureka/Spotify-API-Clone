# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /personalInfoValidation_post for http method type POST in artillery framework

# RoostTestHash=be461d4c4c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "personalInfoValidation_post_success.csv"
    fields:
      - "customerId"
      - "dob"
      - "ssn"
      - "secrectPassword"
      - "AccNumber"
      - "lastDepositAmt"
    skipHeader: true

scenarios:
  - name: "Validate personal info"
    flow:
      - post:
          url: "/personalInfoValidation"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            customerId: "{{ customerId }}"
            dob: "{{ dob }}"
            ssn: "{{ ssn }}"
            secrectPassword: "{{ secrectPassword }}"
            AccNumber: "{{ AccNumber }}"
            lastDepositAmt: "{{ lastDepositAmt }}"
          expect:
            - statusCode: 200
            # Asserting the response headers are present
            - hasProperty: headers
            # Asserting the response body is present
            - hasProperty: body
            # Check the schema properties
            - equals: ["body.customerId", "{{ customerId }}"]
            - equals: ["body.dob", "{{ dob }}"]
            - equals: ["body.ssn", "{{ ssn }}"]
            - equals: ["body.secrectPassword", "{{ secrectPassword }}"]
            - equals: ["body.AccNumber", "{{ AccNumber }}"]
            - equals: ["body.lastDepositAmt", "{{ lastDepositAmt }}"]
          log: "Validated customer personal info"
      - think: 1
