# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=b9ae237532

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "AddRecurringTransfers_post_others.csv"
    fields:
      - "customerId"
      - "amount"
      - "sourceAccountId"
      - "targetAccountId"
      - "currency"
      - "status"
      - "scheduledDate"
      - "recurringOptions"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Attempting to add recurring transfer with invalid koreUserId"
        post:
          url: "/AddRecurringTransfers"
          headers:
            koreUserId: "invalidUser"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            currency: "{{ currency }}"
            status: "{{ status }}"
            scheduledDate: "{{ scheduledDate }}"
            recurringOptions: "{{ recurringOptions }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1

      - log: "Attempting to add recurring transfer with incorrect URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            currency: "{{ currency }}"
            status: "{{ status }}"
            scheduledDate: "{{ scheduledDate }}"
            recurringOptions: "{{ recurringOptions }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1

      - log: "Attempting to add recurring transfer with invalid auth token"
        post:
          url: "/AddRecurringTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidAuthToken"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            currency: "{{ currency }}"
            status: "{{ status }}"
            scheduledDate: "{{ scheduledDate }}"
            recurringOptions: "{{ recurringOptions }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
