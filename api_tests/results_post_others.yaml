# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/results_post for http method type POST in artillery framework
# RoostTestHash=4109b17dda



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "results_post_others.csv"
    fields:
      - "transactionId"
      - "locale"
    skipHeader: true

scenarios:
  - name: "Non-Success API Requests Scenario"
    flow:
      # Invalid URL case
      - post:
          url: "/ship/v1/shipments/invalid-url"
          headers:
            x-customer-transaction-id: "{{ transactionId }}"
            content-type: "application/json"
            x-locale: "{{ locale }}"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          capture:
            json: "$"
          think: 2

      # Invalid payload case
      - post:
          url: "/ship/v1/shipments/results"
          headers:
            x-customer-transaction-id: "{{ transactionId }}"
            content-type: "application/json"
            x-locale: "{{ locale }}"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400
          capture:
            json: "$"
          think: 2

      # Invalid authorization case
      - post:
          url: "/ship/v1/shipments/results"
          headers:
            x-customer-transaction-id: "{{ transactionId }}"
            content-type: "application/json"
            x-locale: "{{ locale }}"
            authorization: "invalidToken"
          expect:
            - statusCode: 401
          capture:
            json: "$"
          think: 2
