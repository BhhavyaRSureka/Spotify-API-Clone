# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetCustomerInfo_post for http method type POST in artillery framework

# RoostTestHash=88810bf424


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetCustomerInfo_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
    skipHeader: true
  headers:
    Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
    X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
    X-CORRELATION-ID: 1234567890

scenarios:
  - name: "Validating successful API requests"
    flow:
      # Request to fetch customer information
      - post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            customerId: 1045300
          capture:
            - json: "$.customerId"
              as: "customerId"
            - json: "$.email"
              as: "email"
            - json: "$.firstName"
              as: "firstName"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "email"
            - hasProperty: "firstName"
        log: "Fetched customer information successfully"
        think: 1
