# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetCustomerInfo_post for http method type POST in artillery framework

# RoostTestHash=88810bf424


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetCustomerInfo_post_success.csv"
    fields:
      - "customerId"
  plugins:
    expect: {}

scenarios:
  - name: "GetCustomerInfo API Test"
    flow:
      - post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{$processEnvironment.koreUserId}}"
            botId: "{{$processEnvironment.botId}}"
            accountId: "{{$processEnvironment.accountId}}"
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
            "X-Auth-token": "{{$processEnvironment.XAuthToken}}"
            "X-CORRELATION-ID": "{{$processEnvironment.XCorrelationId}}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "alternatePhoneNumber"
            - hasProperty: "customerId"
            - hasProperty: "email"
            - hasProperty: "identificationNumber"
            - hasProperty: "identificationType"
            - hasProperty: "image"
            - hasProperty: "isAlternateMobileNumber"
            - hasProperty: "isPrimaryMobileNumber"
            - hasProperty: "location"
            - hasProperty: "name"
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "personalBanker"
            - hasProperty: "phone"
            - hasProperty: "securityQuestions"
            - hasProperty: "segmentId"
            - hasProperty: "userId"
            - hasProperty: "id"
          capture:
            json: "$.customerId"
            as: "customerId"
        think: 1
