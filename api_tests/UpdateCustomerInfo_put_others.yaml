# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=89ddb90785

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 2
  plugins:
    expect: {}
  payload:
    path: "UpdateCustomerInfo_put_others.csv"
    fields:
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
    skipHeader: true

scenarios:
  - name: "Failure case scenarios for UpdateCustomerInfo API"
    flow:
      - log: "Attempt to update customer info with invalid token"
        put:
          url: "/UpdateCustomerInfo"
          headers:
            Authorization: "Invalid Token"
            koreUserId: "fakeUserId"
            botId: "fakeBotId"
            accountId: "fakeAccountId"
            'X-Auth-token': "fakeAuthToken"
            'X-CORRELATION-ID': 1234567890
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
              equals: "Error: Authorisation failed."
        think: 1

      - log: "Attempt to update non-existing customer info"
        put:
          url: "/UpdateCustomerInfo"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "fakeUserId"
            botId: "fakeBotId"
            accountId: "fakeAccountId"
            'X-Auth-token': "fakeAuthToken"
            'X-CORRELATION-ID': 1234567890
          json:
            customerId: 99999999
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
              equals: "Error: Not Found"
        think: 1

      - log: "Attempt to update customer info with invalid URL"
        put:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "fakeUserId"
            botId: "fakeBotId"
            accountId: "fakeAccountId"
            'X-Auth-token': "fakeAuthToken"
            'X-CORRELATION-ID': 1234567890
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 404
        think: 1
