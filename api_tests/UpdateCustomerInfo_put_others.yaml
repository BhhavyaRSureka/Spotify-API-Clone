# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=89ddb90785

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "UpdateCustomerInfo_put_others.csv"
    fields:
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
    skipHeader: true
scenarios:
  - flow:
    - log: "Invalid URL Test"
    - put:
        url: "/UpdateCustomerInfoInvalid"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          botId: "bot123"
          accountId: "account123"
          koreUserId: "koreUser123"
          X-Auth-token: "authToken123"
          X-CORRELATION-ID: 123456789
        json:
          customerId: "{{ customerId }}"
          email: "{{ email }}"
          alternatePhoneNumber: "{{ alternatePhoneNumber }}"
          isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
          phone: "{{ phone }}"
          isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
          location: "{{ location }}"
        expect:
          - statusCode: 404
          - hasProperty: "err.message"
    - think: 1
    - log: "Auth Failure Test"
    - put:
        url: "/UpdateCustomerInfo"
        headers:
          Authorization: "InvalidToken"
          botId: "bot123"
          accountId: "account123"
          koreUserId: "koreUser123"
          X-Auth-token: "authToken123"
          X-CORRELATION-ID: 123456789
        json:
          customerId: "{{ customerId }}"
          email: "{{ email }}"
          alternatePhoneNumber: "{{ alternatePhoneNumber }}"
          isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
          phone: "{{ phone }}"
          isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
          location: "{{ location }}"
        expect:
          - statusCode: 401
          - hasProperty: "err.message"
    - think: 1
    - log: "Invalid Headers Test"
    - put:
        url: "/UpdateCustomerInfo"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          botId: ""
          accountId: ""
          koreUserId: ""
          X-Auth-token: ""
          X-CORRELATION-ID: ""
        json:
          customerId: "{{ customerId }}"
          email: "{{ email }}"
          alternatePhoneNumber: "{{ alternatePhoneNumber }}"
          isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
          phone: "{{ phone }}"
          isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
          location: "{{ location }}"
        expect:
          - statusCode: 401
          - hasProperty: "err.message"
    - think: 1
    - log: "Invalid CustomerId Test"
    - put:
        url: "/UpdateCustomerInfo"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          botId: "bot123"
          accountId: "account123"
          koreUserId: "koreUser123"
          X-Auth-token: "authToken123"
          X-CORRELATION-ID: 123456789
        json:
          customerId: ""
          email: "{{ email }}"
          alternatePhoneNumber: "{{ alternatePhoneNumber }}"
          isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
          phone: "{{ phone }}"
          isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
          location: "{{ location }}"
        expect:
          - statusCode: 404
          - hasProperty: "err.message"
    - think: 1
