# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=adb57fbf1b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "UpdateAccountInfo_put_others.csv"
    fields: ["accountId", "botId", "koreUserId", "Authorization", "X-Auth-token", "X-CORRELATION-ID"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  environments:
    dev:
      target: "http://dev.example.com"
      http:
        pool: 10

scenarios:
  - name: "Test non-success responses for UpdateAccountInfo endpoint"
    flow:
      - log: "Testing with missing required headers"
        put:
          url: "/UpdateAccountInfo"
          headers:
            X-Auth-token: "InvalidToken"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            accountNumber: "1042345"
            customerId: 1045300
            email: "John@gmail.com"
            alternatePhoneNumber: 1234567890
            isAlternateMobileNumber: true
            phone: 9876543210
            isPrimaryMobileNumber: true
            location: "New York"
            isStatement: true
          expect:
            - statusCode: 401
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1

      - log: "Testing with invalid URL"
        put:
          url: "/InvalidURL"
          headers:
            X-Auth-token: "{{ X-Auth-token }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            accountNumber: "1042345"
            customerId: 1045300
            email: "John@gmail.com"
            alternatePhoneNumber: 1234567890
            isAlternateMobileNumber: true
            phone: 9876543210
            isPrimaryMobileNumber: true
            location: "New York"
            isStatement: true
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1
