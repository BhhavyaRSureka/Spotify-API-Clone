# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /DeleteScheduleTransfers_post for http method type POST in artillery framework

# RoostTestHash=47355f872f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "DeleteScheduleTransfers_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
      - "customerId"
      - "paymentId"
      - "updateTransferType"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Delete Schedule Transfers"
    flow:
      - log: "Starting delete schedule transfers test"
      - post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals:
                - "{{ message }}"
                - "Scheduled transfer deleted successfully"
      - think: 1
      - log: "Delete schedule transfers test completed"
