# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /DeleteScheduleTransfers_post for http method type POST in artillery framework

# RoostTestHash=47355f872f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "DeleteScheduleTransfers_post_success.csv"
    fields:
      - "paymentId"
      - "customerId"
      - "updateTransferType"
    skipHeader: true

scenarios:
  - name: "Test DeleteScheduleTransfers API"
    flow:
      - log: "Sending POST request to /DeleteScheduleTransfers"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "{{ $processEnvironment.AUTH_TOKEN }}"
            userCode: "{{ $processEnvironment.AUTH_TOKEN }}"
            accountId: "{{ $processEnvironment.AUTH_TOKEN }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            updateTransferType: "{{ updateTransferType }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
        think: 1

      - log: "Sending POST request to /DeleteScheduleTransfers with invalid payload"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "{{ $processEnvironment.AUTH_TOKEN }}"
            userCode: "{{ $processEnvironment.AUTH_TOKEN }}"
            accountId: "{{ $processEnvironment.AUTH_TOKEN }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: ""
            customerId: ""
            updateTransferType: ""
          expect:
            - statusCode: 400
        think: 1
