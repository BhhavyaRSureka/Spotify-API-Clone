# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=47355f872f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    - path: "DeleteScheduleTransfers_post_others.csv"
      fields:
        - "koreUserId"
        - "botId"
        - "userCode"
        - "accountId"
        - "customerId"
        - "paymentId"
        - "updateTransferType"
      skipHeader: true

scenarios:
  - name: "Test non-success responses for DeleteScheduleTransfers API"
    flow:
      - log: "Sending an invalid request with missing headers"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
            - equals: 
                - "{{ $response.headers.content-type }}"
                - "application/json"
      - think: 5

      - log: "Sending an invalid request with incorrect API endpoint"
        post:
          url: "/InvalidDeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
            - equals: 
                - "{{ $response.headers.content-type }}"
                - "application/json"
      - think: 5

      - log: "Sending an invalid request with incorrect Authorization token"
        post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
            - equals: 
                - "{{ $response.headers.content-type }}"
                - "application/json"
      - think: 5
