# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=47355f872f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 5
      arrivalRate: 1
  payload:
    path: "DeleteScheduleTransfers_post_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
      - "customerId"
      - "paymentId"
      - "updateTransferType"
    skipHeader: true

scenarios:
  - flow:
      - log: "Initiating test case with incorrect URL"
      - get:
          url: "/wrongEndpoint"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 5

      - log: "Initiating test case with invalid payload"
      - post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "invalidPaymentId"
            updateTransferType: "all"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
      - think: 5

      - log: "Initiating test case with invalid authorization token"
      - post:
          url: "/DeleteScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "invalidToken"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "all"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 5
