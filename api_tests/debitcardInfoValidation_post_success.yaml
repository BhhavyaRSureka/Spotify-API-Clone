# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /debitcardInfoValidation_post for http method type POST in artillery framework

# RoostTestHash=332e5b852f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "debitcardInfoValidation_post_success.csv"
    fields:
      - "debitCardNumber"
      - "pin"
      - "cvv"
      - "expiryDate"
      - "customerId"
    skipHeader: true
  environments:
    dev:
      target: "http://dev.example.com"
    prod:
      target: "http://prod.example.com"

scenarios:
  - name: "Validating debit card info"
    flow:
      - log: "Starting the debit card info validation process"
      - post:
          url: "/debitcardInfoValidation"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            debitCardNumber: "{{ debitCardNumber }}"
            pin: "{{ pin }}"
            cvv: "{{ cvv }}"
            expiryDate: "{{ expiryDate }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "content"
          # Add a think time
          think: 1
      - log: "Debit card info validated successfully"
