# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# Test generated for /debitcardInfoValidation_post for http method type POST in artillery framework
# RoostTestHash=332e5b852f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 10
  payload:
    path: "debitcardInfoValidation_post_success.csv"
    fields:
      - "customerId"
      - "cvv"
      - "debitCardNumber"
      - "expiryDate"
      - "pin"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Debit Card Information Validation"
    flow:
      - log: "Testing Debit Card Information Validation API"
      - post:
          url: "/debitcardInfoValidation"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            customerId: "{{ customerId }}"
            cvv: "{{ cvv }}"
            debitCardNumber: "{{ debitCardNumber }}"
            expiryDate: "{{ expiryDate }}"
            pin: "{{ pin }}"
          expect:
            - statusCode: 200
            - hasProperty: "err"
          # The API should return a success status code and a property named err in the response body
          # for successful validation of debit card information
      - think: 1
