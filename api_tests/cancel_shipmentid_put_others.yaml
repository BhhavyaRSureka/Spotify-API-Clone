# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/tag/cancel/{shipmentid}_put for http method type PUT in artillery framework
# RoostTestHash=dfff906444



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "cancel_shipmentid_put_others.csv"
    fields:
      - "shipmentid"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Failure scenario for shipment cancellation"
    flow:
      # Invalid URL
      - get:
          url: "/invalid/url"
          expect:
            - statusCode: 404
          think: 1
      # Invalid payload
      - put:
          url: "/ship/v1/shipments/tag/cancel/{{ shipmentid }}"
          headers:
            content-type: "application/json"
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400
          think: 1
      # Missing required header
      - put:
          url: "/ship/v1/shipments/tag/cancel/{{ shipmentid }}"
          headers:
            content-type: "application/json"
          json:
            shipmentid: "{{ shipmentid }}"
          expect:
            - statusCode: 401
          think: 1
      # Unauthorized request
      - put:
          url: "/ship/v1/shipments/tag/cancel/{{ shipmentid }}"
          headers:
            content-type: "application/json"
            authorization: "Bearer invalidToken"
          json:
            shipmentid: "{{ shipmentid }}"
          expect:
            - statusCode: 403
          think: 1
      # Not found
      - put:
          url: "/ship/v1/shipments/tag/cancel/invalidShipmentId"
          headers:
            content-type: "application/json"
            authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            shipmentid: "invalidShipmentId"
          expect:
            - statusCode: 404
          think: 1
