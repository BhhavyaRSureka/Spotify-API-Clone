# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /resetPassword_post for http method type POST in artillery framework

# RoostTestHash=79c3f016d8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "resetPassword_post_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Reset Password API Test"
    flow:
      - log: "Attempting to reset password with valid credentials"
        post:
          url: "/resetPassword"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals:
                - "message"
                - "password has been updated successfully."
          think: 1
