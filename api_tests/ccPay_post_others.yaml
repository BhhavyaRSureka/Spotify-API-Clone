# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c6fa681866

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "ccPay_post_others.csv"
    fields:
      - "amount"
      - "currency"
      - "customerId"
      - "dueDate"
      - "sourceAccountId"
      - "targetAccountId"
      - "transactionType"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "ccPay API test"
    flow:
      - log: "Test Case 1: Test with invalid Authorization token"
        post:
          url: "/ccPay"
          headers:
            koreUserId: "KoreUser1"
            botId: "Bot1"
            accountId: "Account1"
            Authorization: "InvalidToken"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            transactionType: "{{ transactionType }}"
            dueDate: "{{ dueDate }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
            - equals: 
              - "{{ response.body.err.message }}"
              - "Error: Authorisation failed."
      - think: 1

      - log: "Test Case 2: Test with missing required header parameters"
        post:
          url: "/ccPay"
          headers:
            koreUserId: "KoreUser1"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            transactionType: "{{ transactionType }}"
            dueDate: "{{ dueDate }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
            - equals: 
              - "{{ response.body.err.message }}"
              - "Error: Not Found"
      - think: 1

      - log: "Test Case 3: Test with invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "KoreUser1"
            botId: "Bot1"
            accountId: "Account1"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            transactionType: "{{ transactionType }}"
            dueDate: "{{ dueDate }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
            - equals: 
              - "{{ response.body.err.message }}"
              - "Error: Not Found"
      - think: 1
