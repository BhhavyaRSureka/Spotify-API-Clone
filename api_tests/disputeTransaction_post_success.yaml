# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# Test generated for /disputeTransaction_post for http method type POST in artillery framework
# RoostTestHash=a16706fdab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "disputeTransaction_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "customerId"
      - "emailId"
      - "transactionId"
      - "reasonCode"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Dispute transaction test"
    flow:
      - log: "Starting dispute transaction test"
      - post:
          url: "/disputeTransaction"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            emailId: "{{ emailId }}"
            transactionsData:
              - transactionId: "{{ transactionId }}"
                reasonCode: "{{ reasonCode }}"
          expect:
            - statusCode: 200
            - hasProperty: "content"
          capture:
            - json: "$.content"
              as: "responseBody"
      - think: 1
      - log: "Dispute transaction test completed"
