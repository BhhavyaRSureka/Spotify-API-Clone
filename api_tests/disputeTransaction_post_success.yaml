# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /disputeTransaction_post for http method type POST in artillery framework

# RoostTestHash=a16706fdab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "disputeTransaction_post_success.csv"
    fields:
      - "customerId"
      - "emailId"
      - "transactionId"
      - "reasonCode"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      auth_token: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Dispute Transaction API Test"
    flow:
      - log: "Dispute Transaction API Test Started"
      - post:
          url: "/disputeTransaction"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $environments.dev.auth_token }}"
          json:
            customerId: "{{ customerId }}"
            emailId: "{{ emailId }}"
            transactionsData: 
              - transactionId: "{{ transactionId }}"
                reasonCode: "{{ reasonCode }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
            - equals:
                - "{{ description }}"
                - "Dispute Transactions reported successfully"
      - think: 1
      - log: "Dispute Transaction API Test Ended"
