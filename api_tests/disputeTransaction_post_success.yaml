# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /disputeTransaction_post for http method type POST in artillery framework

# RoostTestHash=a16706fdab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "disputeTransaction_post_success.csv"
    fields:
      - "customerId"
      - "emailId"
      - "transactionsData"
    skipHeader: true
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"

scenarios:
  - name: "Dispute Transaction Scenario"
    flow:
      - log: "Starting Dispute Transaction test..."
      - post:
          url: "/disputeTransaction"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            emailId: "{{ emailId }}"
            transactionsData: "{{ transactionsData }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "emailId"
            - hasProperty: "transactionsData"
          # Asserting the schema property format and pattern, along with data type
            - equals: ["{{ contentType }}", "application/json"]
      - think: 1
      - log: "Dispute Transaction test completed successfully."
