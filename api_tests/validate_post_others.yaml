# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/packages/validate_post for http method type POST in artillery framework
# RoostTestHash=cda593a0d1



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "validate_post_others.csv"
    fields:
      - "x-customer-transaction-id"
      - "content-type"
      - "x-locale"
      - "authorization"
    skipHeader: true
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Test scenario for non-successful API requests"
    flow:
      # Test case for Invalid payload
      - post:
          url: "/ship/v1/shipments/packages/validate"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ authorization }}"
          json:
            invalidPayload: "invalid"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1

      # Test case for missing required header
      - post:
          url: "/ship/v1/shipments/packages/validate"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ authorization }}"
          json:
            validPayload: "valid"
          expect:
            - statusCode: 400
            - hasProperty: "message"
          think: 1

      # Test case for Invalid URL
      - post:
          url: "/ship/v1/shipments/package/validate" # Invalid URL
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ authorization }}"
          json:
            validPayload: "valid"
          expect:
            - statusCode: 404
          think: 1
