# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/tag_post for http method type POST in artillery framework
# RoostTestHash=cc7400f160



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "tag_post_success.csv"
    fields:
      - "x-customer-transaction-id"
      - "content-type"
      - "x-locale"
      - "authorization"
    skipHeader: true 
    order: sequence

scenarios:
  - name: "API test scenario for non-success responses"
    flow:
      # POST request to /ship/v1/shipments/tag
      - post:
          url: "/ship/v1/shipments/tag"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ authorization }}"
          expect:
            - statusCode: 200
            - hasProperty: "transactionId"
            - hasProperty: "customerTransactionId"
            - hasProperty: "output"
            - hasProperty: "output.masterTrackingNumber"
            - hasProperty: "output.serviceType"
            - hasProperty: "output.shipTimestamp"
            - hasProperty: "output.alerts"
            - hasProperty: "output.alerts.0"
          # Think time after each request
          think: 1
