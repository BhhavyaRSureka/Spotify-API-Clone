# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetAccountInfo_post for http method type POST in artillery framework

# RoostTestHash=8a95fbc531


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetAccountInfo_post_success.csv"
    fields:
      - "customerId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "Get Account Info Test"
    flow:
      - log: "Running GET /GetAccountInfo request"
      - get:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ $processEnvironment.KORE_USER_ID }}"
            botId: "{{ $processEnvironment.BOT_ID }}"
            accountId: "{{ $processEnvironment.ACCOUNT_ID }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "{{ $processEnvironment.CORRELATION_ID }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "accounts"
      - think: 5
      - log: "Running POST /GetAccountInfo request"
      - post:
          url: "/GetAccountInfo"
          json:
            customerId: "{{ customerId }}"
          headers:
            koreUserId: "{{ $processEnvironment.KORE_USER_ID }}"
            botId: "{{ $processEnvironment.BOT_ID }}"
            accountId: "{{ $processEnvironment.ACCOUNT_ID }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "{{ $processEnvironment.CORRELATION_ID }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "accounts"
      - think: 5
