# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /deposit_post for http method type POST in artillery framework

# RoostTestHash=70db6d5eac


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "deposit_post_success.csv"
    fields: ["customerId", "sourceAccountId", "amount", "currency", "frontImage", "backImage"]
    skipHeader: true
scenarios:
  - name: "Successful API requests"
    flow:
      - log: "Making a check deposit on a user's account"
      - post:
          url: "/deposit"
          headers:
            koreUserId: "{{ $processEnvironment.KORE_USER_ID }}"
            botId: "{{ $processEnvironment.BOT_ID }}"
            accountId: "{{ $processEnvironment.ACCOUNT_ID }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
            - hasProperty: "content"
      - think: 1
