# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /deposit_post for http method type POST in artillery framework

# RoostTestHash=70db6d5eac


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  override:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "deposit_post_success.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true

scenarios:
  - name: "Test API behavior with valid payloads"
    flow:
      - post:
          url: "/deposit"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          headers:
            koreUserId: "testUser"
            botId: "testBot"
            accountId: "testAccount"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
            - equals:
                description: "Deposit success"
          capture:
            - json: "$.description"
              as: "description"
      - think: 1
      - log: "Verified deposit request"
