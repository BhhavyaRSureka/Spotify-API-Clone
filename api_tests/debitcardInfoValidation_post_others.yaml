# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=332e5b852f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "debitcardInfoValidation_post_others.csv"
    fields:
      - "customerId"
      - "cvv"
      - "debitCardNumber"
      - "expiryDate"
      - "pin"
    skipHeader: true

scenarios:
  - name: "API Failure Scenarios"
    flow:
      - log: "Testing with invalid payload"
        post:
          url: "/debitcardInfoValidation"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            debitCardNumber: "invalid"
            pin: "12345"
            cvv: "1234"
            expiryDate: "2025-01-01T00:00:00.000+0000"
            customerId: 1045300
          expect:
            - statusCode: 400 
            - hasProperty: "err.message"
      - think: 5
      - log: "Testing with invalid authorization"
        post:
          url: "/debitcardInfoValidation"
          headers:
            Authorization: "Bearer invalidToken"
          json:
            debitCardNumber: "1234456778901690"
            pin: "1111"
            cvv: "123"
            expiryDate: "2025-01-01T00:00:00.000+0000"
            customerId: 1045300
          expect:
            - statusCode: 401 
            - hasProperty: "err.message"
      - think: 5
      - log: "Testing with non-existing endpoint"
        post:
          url: "/nonExistingEndpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            debitCardNumber: "1234456778901690"
            pin: "1111"
            cvv: "123"
            expiryDate: "2025-01-01T00:00:00.000+0000"
            customerId: 1045300
          expect:
            - statusCode: 404 
            - hasProperty: "err.message"
