# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/tag/cancel/{shipmentid}_put for http method type PUT in artillery framework
# RoostTestHash=dfff906444



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "cancel_shipmentid_put_success.csv"
    fields:
      - "shipmentid"
    skipHeader: true

scenarios:
  - name: "Cancel shipment API test scenario"
    flow:
      - put:
          url: "/ship/v1/shipments/tag/cancel/{{ shipmentid }}"
          headers:
            x-customer-transaction-id: "sample_transaction_id"
            content-type: "application/json"
            x-locale: "en-US"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "transactionId"
            - hasProperty: "customerTransactionId"
            - hasProperty: "output"
            - hasProperty: "output.cancelledTag"
            - hasProperty: "output.successMessage"
      - think: 1
