# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c0505709a3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "UpdateScheduleTransfers_put_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
      - "paymentId"
      - "customerId"
      - "amount"
      - "scheduledDate"
      - "sourceAccountId"
      - "targetAccountId"
    skipHeader: true
scenarios:
  - name: "UpdateScheduleTransfers API test"
    flow:
      # Test case for 401 error response with invalid Authorization token
      - post:
          url: "/UpdateScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
          log: "Testing 401 error response with invalid Authorization token"
      
      # Test case for 404 error response with invalid URL
      - post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
          log: "Testing 404 error response with invalid URL"

      # Test case for 404 error response with missing required header parameters
      - post:
          url: "/UpdateScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            userCode: "{{ userCode }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
          log: "Testing 404 error response with missing required header parameters"

      # Test case for 404 error response with missing required request body parameters
      - post:
          url: "/UpdateScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            amount: "{{ amount }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
          log: "Testing 404 error response with missing required request body parameters"
