# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=c0505709a3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "UpdateScheduleTransfers_put_others.csv"
    fields:
      - "paymentId"
      - "customerId"
      - "amount"
      - "scheduledDate"
      - "sourceAccountId"
      - "targetAccountId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Testing non-success responses for UpdateScheduleTransfers API"
    flow:
      - log: "Sending a request with a missing required header"
        put:
          url: "/UpdateScheduleTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: ""
            userCode: "userCode"
            accountId: "accountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            scheduledDate: "{{ scheduledDate }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1

      - log: "Sending a request to an invalid URL"
        put:
          url: "/InvalidURL"
          headers:
            koreUserId: "bhhavvya"
            botId: "botId"
            userCode: "userCode"
            accountId: "accountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            scheduledDate: "{{ scheduledDate }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1

      - log: "Sending a request with invalid authorization credentials"
        put:
          url: "/UpdateScheduleTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "botId"
            userCode: "userCode"
            accountId: "accountId"
            Authorization: "InvalidToken"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            scheduledDate: "{{ scheduledDate }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
