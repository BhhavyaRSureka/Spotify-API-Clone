# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# Test generated for /UpdateAccountInfo_put for http method type PUT in artillery framework
# RoostTestHash=adb57fbf1b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my-processor.js"
  plugins:
    expect: {}
  payload:
    path: "UpdateAccountInfo_put_success.csv"
    fields: 
      - "koreUserId"
      - "botId"
      - "accountId"
      - "accountNumber"
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
      - "isStatement"
    skipHeader: true
scenarios:
  - name: "Update Account Info Test Scenario"
    flow:
      - log: "Starting Update Account Info Test Scenario"
      - post:
          url: "/UpdateAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "{{ $randomNumber(1000000000000,9999999999999) }}"
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          capture:
            - json: "$.id"
              as: "accountId"
          expect:
            - statusCode: 200
            - hasProperty: "accountId"
          log: "Update Account Info API request sent successfully"
      - think: 1
