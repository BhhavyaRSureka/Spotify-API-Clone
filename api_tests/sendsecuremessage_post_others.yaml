# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3b0ac86130

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "sendsecuremessage_post_others.csv"
    fields:
      - "authType"
      - "firstName"
      - "lastName"
      - "email"
      - "phoneNumber"
      - "topic"
      - "message"
      - "availableTime"
      - "chatHistory"
      - "cif"
    skipHeader: true

scenarios:
  - name: "SecureMessage API scenario"
    flow:
      - log: "Sending request with invalid authorization token"
        post:
          url: "/sendsecuremessage"
          headers:
            Authorization: "InvalidToken"
          json:
            authType: "{{ authType }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            phoneNumber: "{{ phoneNumber }}"
            topic: "{{ topic }}"
            message: "{{ message }}"
            availableTime: "{{ availableTime }}"
            chatHistory: "{{ chatHistory }}"
            cif: "{{ cif }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
        think: 1

      - log: "Sending request with missing required field"
        post:
          url: "/sendsecuremessage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            authType: "{{ authType }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            phoneNumber: "{{ phoneNumber }}"
            topic: "{{ topic }}"
            message: "{{ message }}"
            availableTime: "{{ availableTime }}"
            chatHistory: "{{ chatHistory }}"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
        think: 1

      - log: "Sending request with invalid URL"
        post:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            authType: "{{ authType }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            phoneNumber: "{{ phoneNumber }}"
            topic: "{{ topic }}"
            message: "{{ message }}"
            availableTime: "{{ availableTime }}"
            chatHistory: "{{ chatHistory }}"
            cif: "{{ cif }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
        think: 1
