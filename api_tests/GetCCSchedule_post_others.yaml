# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=b38ec5fe10

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetCCSchedule_post_others.csv"
    fields: ["customerId", "status"]
    skipHeader: true
  phases:
    - duration: 120
      arrivalRate: 10

scenarios:
  - name: "Non-Success API Responses Testing"
    flow:
      - log: "Sending incorrect payload"
        post:
          url: "/GetCCSchedule"
          headers:
            koreUserId: "bhhavvya"
            botId: "123"
            accountId: "123"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "abc" # Expected to fail as customerId should be a number
            status: "Pending"
          expect:
            - statusCode: 400 # Expecting Bad Request as payload is incorrect
            - hasProperty: "err.message"
      - think: 5

      - log: "Sending request without authorization"
        post:
          url: "/GetCCSchedule"
          headers:
            koreUserId: "bhhavvya"
            botId: "123"
            accountId: "123"
          json:
            customerId: 1045300
            status: "Pending"
          expect:
            - statusCode: 401 # Expecting Unauthorized as no authorization token is provided
            - hasProperty: "err.message"
      - think: 5

      - log: "Sending request to incorrect URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "bhhavvya"
            botId: "123"
            accountId: "123"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: 1045300
            status: "Pending"
          expect:
            - statusCode: 404 # Expecting Not Found as URL is incorrect
            - hasProperty: "err.message"
      - think: 5
