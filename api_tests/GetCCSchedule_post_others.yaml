# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=b38ec5fe10

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetCCSchedule_post_others.csv"
    fields:
      - "customerId"
      - "status"
    skipHeader: true
  environments:
    prod:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 60
          arrivalRate: 5
scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending a request with incorrect payload"
        post:
          url: "/GetCCSchedule"
          headers:
            koreUserId: "invalid"
            botId: "invalid"
            accountId: "invalid"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "invalid"
            status: "invalid"
          expect:
            - statusCode: 400
            - hasProperty: "err"
        think: 5
      - log: "Sending a request with incorrect URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "invalid"
            botId: "invalid"
            accountId: "invalid"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "invalid"
            status: "invalid"
          expect:
            - statusCode: 404
            - hasProperty: "err"
        think: 5
      - log: "Sending a request with invalid Authorization"
        post:
          url: "/GetCCSchedule"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "invalid"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "err"
        think: 5
