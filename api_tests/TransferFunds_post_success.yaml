# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# Test generated for /TransferFunds_post for http method type POST in artillery framework
# RoostTestHash=289ed66c26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "TransferFunds_post_success.csv"
    fields:
      - "customerId"
      - "amount"
      - "currency"
      - "sourceAccountId"
      - "targetAccountId"
      - "date"
      - "description"
      - "TransactionType"
      - "PaymentType"
    skipHeader: true

scenarios:
  - name: "Transfer funds between accounts"
    flow:
      - post:
          url: "/TransferFunds"
          headers:
            koreUserId: "testUser"
            botId: "testBot"
            userCode: "testUser"
            accountId: "testAccount"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 200
          log: "Transferring funds between accounts"
      - think: 1
