# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /TransferFunds_post for http method type POST in artillery framework

# RoostTestHash=289ed66c26


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "TransferFunds_post_success.csv"
    fields: ["koreUserId", "botId", "userCode", "accountId", "Authorization", "customerId", "amount", "currency", "sourceAccountId", "targetAccountId", "date", "description", "TransactionType", "PaymentType"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      auth: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Transfer funds between accounts"
    flow:
      - post:
          url: "/TransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "amount"
            - hasProperty: "currency"
            - hasProperty: "sourceAccountId"
            - hasProperty: "targetAccountId"
            - hasProperty: "date"
            - hasProperty: "description"
            - hasProperty: "TransactionType"
            - hasProperty: "PaymentType"
      - think: 1
