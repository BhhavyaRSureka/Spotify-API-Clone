# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=7e03af5a84

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetRecurringTransfers_post_others.csv"
    fields:
      - "customerId"
      - "status"
    skipHeader: true

scenarios:
  - name: "Non-Success API Response Test Scenario"
    flow:
      - log: "Sending a POST request with invalid headers"
        post:
          url: "/GetRecurringTransfers"
          headers:
            koreUserId: "invalid"
            botId: "invalid"
            userCode: "invalid"
            accountId: "invalid"
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
      - log: "Sending a POST request with an invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1
      - log: "Sending a POST request with invalid payload"
        post:
          url: "/GetRecurringTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "invalid"
            status: "invalid"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
