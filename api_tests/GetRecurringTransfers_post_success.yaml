# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetRecurringTransfers_post for http method type POST in artillery framework

# RoostTestHash=7e03af5a84


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "GetRecurringTransfers_post_success.csv"
    fields:
      - "customerId"
      - "status"
    skipHeader: true

scenarios:
  - name: "Test GetRecurringTransfers API"
    flow:
      # This request fetches all the recurring transfers
      - post:
          url: "/GetRecurringTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            # Assert that the response has specific headers
            - hasHeader: "content-type"
            # Assert that the response body has specific properties
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "DueDate"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "ScheduledDate"
            - hasProperty: "recurringOptions"
            - hasProperty: "id"
          capture:
            - json: "$.Type"
              as: "Type"
            - json: "$.CustomerId"
              as: "CustomerId"
            - json: "$.Amount"
              as: "Amount"
            - json: "$.SourceAccountNumber"
              as: "SourceAccountNumber"
            - json: "$.TargetAccountNumber"
              as: "TargetAccountNumber"
            - json: "$.DueDate"
              as: "DueDate"
            - json: "$.Currency"
              as: "Currency"
            - json: "$.Status"
              as: "Status"
            - json: "$.ScheduledDate"
              as: "ScheduledDate"
            - json: "$.recurringOptions"
              as: "recurringOptions"
            - json: "$.id"
              as: "id"
      - log: "Fetched all the recurring transfers"
      - think: 1
