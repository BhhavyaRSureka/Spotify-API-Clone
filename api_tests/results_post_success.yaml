# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/results_post for http method type POST in artillery framework
# RoostTestHash=4109b17dda



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "results_post_success.csv"
    fields:
      - "x-customer-transaction-id"
      - "content-type"
      - "x-locale"
      - "authorization"
    skipHeader: true

scenarios:
  - name: "Test POST /ship/v1/shipments/results API"
    flow:
      - post:
          url: "/ship/v1/shipments/results"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "transactionId"
            - hasProperty: "customerTransactionId"
            - hasHeader: "No response headers"
          think: 1
