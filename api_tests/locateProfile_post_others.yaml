# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2b60fcac41

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "locateProfile_post_others.csv"
    fields:
      - "customerId"
      - "ssNumber"
      - "ssnShort"
      - "accountNumber"
    skipHeader: true
  http:
    timeout: 10
scenarios:
  - name: "Test locateProfile endpoint with error conditions"
    flow:
      - log: "Sending POST request with invalid payload"
      - post:
          url: "/locateProfile"
          json:
            customerId: "invalid"
            ssNumber: "invalid"
            ssnShort: "invalid"
            accountNumber: "invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 5
      - log: "Sending POST request with invalid Authorization"
      - post:
          url: "/locateProfile"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 5
      - log: "Sending POST request to invalid URL"
      - post:
          url: "/invalidURL"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
