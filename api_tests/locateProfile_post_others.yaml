# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=2b60fcac41

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "locateProfile_post_others.csv"
    fields:
      - "customerId"
      - "ssNumber"
      - "ssnShort"
      - "accountNumber"
    skipHeader: true

scenarios:
  - name: "Error Scenarios for locateProfile API"
    flow:
      - log: "Testing with incorrect URL"
        post:
          url: "/incorrectURL"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - equals:
                err.message: "Not Found"
        think: 5

      - log: "Testing with invalid authorization token"
        post:
          url: "/locateProfile"
          headers:
            Authorization: "Bearer invalid_token"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
            accountNumber: "{{ accountNumber }}"
          expect:
            - statusCode: 401
            - hasProperty: "err"
            - equals:
                err.message: "Authorization failed"
        think: 5

      - log: "Testing with missing required parameters"
        post:
          url: "/locateProfile"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            ssNumber: "{{ ssNumber }}"
            ssnShort: "{{ ssnShort }}"
          expect:
            - statusCode: 400
            - hasProperty: "err"
            - equals:
                err.message: "Missing required parameter: accountNumber"
        think: 5
