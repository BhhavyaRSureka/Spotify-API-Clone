# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=289ed66c26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "TransferFunds_post_others.csv"
    fields: ["customerId", "amount", "currency", "sourceAccountId", "targetAccountId", "date", "description", "TransactionType", "PaymentType"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 2

scenarios:
  - name: "Test API failure responses"
    flow:
      # Test case: Invalid payload
      - post:
          url: "/TransferFunds"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          headers:
            koreUserId: "invalid"
            botId: "invalid"
            userCode: "invalid"
            accountId: "invalid"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.err.message"
          expect:
            - statusCode: 401
      - think: 1

      # Test case: Invalid URL
      - post:
          url: "/InvalidURL"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          headers:
            koreUserId: "valid"
            botId: "valid"
            userCode: "valid"
            accountId: "valid"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      # Test case: Invalid authorization
      - post:
          url: "/TransferFunds"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          headers:
            koreUserId: "valid"
            botId: "valid"
            userCode: "valid"
            accountId: "valid"
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1
