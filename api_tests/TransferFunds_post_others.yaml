# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=289ed66c26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "TransferFunds_post_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "TransferFunds API failure cases testing"
    flow:
      - log: "Testing for invalid authorization"
        post:
          url: "/TransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
          json:
            customerId: 1045300
            amount: 3
            currency: "USD"
            sourceAccountId: "1042345"
            targetAccountId: "1042222"
            date: "03-15-2021"
            description: "Internal Transfer"
            TransactionType: "1063"
            PaymentType: "Minimum Payment"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
      - log: "Testing for invalid endpoint"
        post:
          url: "/InvalidTransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: 1045300
            amount: 3
            currency: "USD"
            sourceAccountId: "1042345"
            targetAccountId: "1042222"
            date: "03-15-2021"
            description: "Internal Transfer"
            TransactionType: "1063"
            PaymentType: "Minimum Payment"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1
      - log: "Testing for missing required header"
        post:
          url: "/TransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: 1045300
            amount: 3
            currency: "USD"
            sourceAccountId: "1042345"
            targetAccountId: "1042222"
            date: "03-15-2021"
            description: "Internal Transfer"
            TransactionType: "1063"
            PaymentType: "Minimum Payment"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1
      - log: "Testing for missing required body field"
        post:
          url: "/TransferFunds"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: 1045300
            currency: "USD"
            sourceAccountId: "1042345"
            targetAccountId: "1042222"
            date: "03-15-2021"
            description: "Internal Transfer"
            TransactionType: "1063"
            PaymentType: "Minimum Payment"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
