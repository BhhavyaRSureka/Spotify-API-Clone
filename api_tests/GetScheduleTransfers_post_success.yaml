# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetScheduleTransfers_post for http method type POST in artillery framework

# RoostTestHash=7b4d2d87f9


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetScheduleTransfers_post_success.csv"
    fields: ["koreUserId", "botId", "userCode", "accountId", "Authorization"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Validate successful API requests"
    flow:
      - log: "Starting test for GetScheduleTransfers API"
      - post:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: 1045300
            status: "Pending"
          expect:
            - statusCode: 200
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "DueDate"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "ScheduledDate"
            - hasProperty: "NextScheduleDate"
            - hasProperty: "id"
          capture:
            - json: "$.Type"
              as: "Type"
            - json: "$.CustomerId"
              as: "CustomerId"
            - json: "$.Amount"
              as: "Amount"
            - json: "$.SourceAccountNumber"
              as: "SourceAccountNumber"
            - json: "$.TargetAccountNumber"
              as: "TargetAccountNumber"
            - json: "$.DueDate"
              as: "DueDate"
            - json: "$.Currency"
              as: "Currency"
            - json: "$.Status"
              as: "Status"
            - json: "$.ScheduledDate"
              as: "ScheduledDate"
            - json: "$.NextScheduleDate"
              as: "NextScheduleDate"
            - json: "$.id"
              as: "id"
      - think: 1
