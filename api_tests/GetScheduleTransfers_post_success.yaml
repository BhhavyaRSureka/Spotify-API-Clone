# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# Test generated for /GetScheduleTransfers_post for http method type POST in artillery framework
# RoostTestHash=7b4d2d87f9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetScheduleTransfers_post_success.csv"
    fields: ["koreUserId", "botId", "userCode", "accountId", "customerId", "status"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Validate GetScheduleTransfers API"
    flow:
      - post:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "{{koreUserId}}"
            botId: "{{botId}}"
            userCode: "{{userCode}}"
            accountId: "{{accountId}}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{customerId}}"
            status: "{{status}}"
          log: "Posting request to get scheduled transfers"
          expect:
            - statusCode: 200
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "DueDate"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "ScheduledDate"
            - hasProperty: "NextScheduleDate"
            - hasProperty: "id"
      - think: 1
