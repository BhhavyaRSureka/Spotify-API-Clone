# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/cancel_put for http method type PUT in artillery framework
# RoostTestHash=afa7829880



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "cancel_put_success.csv"
    fields:
      - "x-customer-transaction-id"
      - "content-type"
      - "x-locale"
      - "authorization"
    skipHeader: true

scenarios:
  - name: "Test Scenario for Non-Success API Responses"
    flow:
      # Put request to cancel shipment
      - put:
          url: "/ship/v1/shipments/cancel"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ authorization }}"
          expect:
            - statusCode: 200
            - hasProperty: "transactionId"
            - hasProperty: "customerTransactionId"
            - hasProperty: "output"
            - hasProperty: "output.cancelledShipment"
            - hasProperty: "output.cancelledHistory"
            - hasProperty: "output.successMessage"
            - hasProperty: "output.alerts"
            - hasProperty: "output.alerts.0"
          think: 1
