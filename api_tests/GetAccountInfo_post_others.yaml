# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=8a95fbc531

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetAccountInfo_post_others.csv"
    fields: ["customerId", "koreUserId", "botId", "accountId"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Testing failure cases for GetAccountInfo endpoint"
    flow:
      - log: "Sending request with invalid customerId"
        post:
          url: "/GetAccountInfo"
          headers:
            Authorization: "Bearer invalid_token"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            customerId: "invalidCustomerId"
        expect:
          - statusCode: 400
            # The API should respond with a 400 status code for invalid requests

      - think: 1

      - log: "Sending request with missing botId"
        post:
          url: "/GetAccountInfo"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "{{ koreUserId }}"
            botId: ""
            accountId: "{{ accountId }}"
          json:
            customerId: "{{ customerId }}"
        expect:
          - statusCode: 400
            # The API should respond with a 400 status code for missing required headers

      - think: 1

      - log: "Sending request with incorrect URL"
        post:
          url: "/IncorrectURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            customerId: "{{ customerId }}"
        expect:
          - statusCode: 404
            # The API should respond with a 404 status code for incorrect URL

      - think: 1

      - log: "Sending request with invalid Authorization token"
        post:
          url: "/GetAccountInfo"
          headers:
            Authorization: "Bearer invalid_token"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            customerId: "{{ customerId }}"
        expect:
          - statusCode: 401
            # The API should respond with a 401 status code for invalid Authorization token
