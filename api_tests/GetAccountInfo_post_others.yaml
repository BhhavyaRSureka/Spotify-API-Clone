# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=8a95fbc531

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetAccountInfo_post_others.csv"
    fields:
      - "botId"
      - "accountId"
      - "customerId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Sending request with missing required header"
        post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "bhhavvya"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "invalid_token"
            X-CORRELATION-ID: 1234567890
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
        # Expecting a 401 response due to invalid X-Auth-token
      - think: 1

      - log: "Sending request with an invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "valid_token"
            X-CORRELATION-ID: 1234567890
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
        # Expecting a 404 response due to invalid URL
      - think: 1

      - log: "Sending request with incorrect payload"
        post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "valid_token"
            X-CORRELATION-ID: 1234567890
          json:
            customerId: "invalid_customerId"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
        # Expecting a 400 response due to invalid customerId in the payload
      - think: 1
