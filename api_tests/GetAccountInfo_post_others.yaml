# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=8a95fbc531

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetAccountInfo_post_others.csv"
    fields: ["koreUserId", "botId", "accountId"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10

scenarios:
  - name: "Non-Success API Responses Scenario"
    flow:
      - log: "Sending a request with incorrect authorization header"
        post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "1234567890"
          json:
            customerId: 1045300
          expect:
            - statusCode: 401
            - hasProperty: ['err', 'message']
      - think: 1

      - log: "Sending a request with non-existing account ID"
        post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "NonExistingAccountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "1234567890"
          json:
            customerId: 1045300
          expect:
            - statusCode: 404
            - hasProperty: ['err', 'message']
      - think: 1

      - log: "Sending a request to an invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "1234567890"
          json:
            customerId: 1045300
          expect:
            - statusCode: 404
            - hasProperty: ['err', 'message']
      - think: 1
