# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateRecurringTransfers_put for http method type PUT in artillery framework

# RoostTestHash=d94103a8c3


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "UpdateRecurringTransfers_put_success.csv"
    fields: ["customerId", "paymentId", "amount", "scheduledDate", "sourceAccountId", "targetAccountId", "amountType", "frequency", "iterate", "numberOfTransfers", "updateTransferType"]
    skipHeader: true
  environments:
    dev:
      target: "http://dev.example.com"
    prod:
      target: "http://prod.example.com"

scenarios:
  - name: "Update Recurring Transfers"
    flow:
      - log: "Starting Update Recurring Transfers test scenario"
      - put:
          url: "/UpdateRecurringTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            amount: "{{ amount }}"
            scheduledDate: "{{ scheduledDate }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            recurringOptions:
              amountType: "{{ amountType }}"
              frequency:
                iterate: "{{ iterate }}"
                duration:
                  numberOfTransfers: "{{ numberOfTransfers }}"
            updateTransferType: "{{ updateTransferType }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: ["{{ message }}", "Recurring transfer updated successfully"]
      - think: 1
