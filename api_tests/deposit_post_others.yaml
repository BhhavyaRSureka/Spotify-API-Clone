# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=70db6d5eac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "deposit_post_others.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true

scenarios:
  - name: "Test deposit API with failure cases"
    flow:
      - log: "Sending request with incorrect URL"
        post:
          url: "/incorrect/deposit"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "bot123"
            accountId: "account123"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
        think: 1

      - log: "Sending request with invalid Authorization token"
        post:
          url: "/deposit"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "bot123"
            accountId: "account123"
            Authorization: "invalid_token"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
        think: 1

      - log: "Sending request with missing required header"
        post:
          url: "/deposit"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "bot123"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
        think: 1

      - log: "Sending request with invalid payload"
        post:
          url: "/deposit"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "bot123"
            accountId: "account123"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "invalid_customer_id"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 400
            - hasProperty: "err.message"
        think: 1
