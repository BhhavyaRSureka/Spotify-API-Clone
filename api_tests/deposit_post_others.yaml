# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=70db6d5eac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "deposit_post_others.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending an invalid request with incorrect payload"
        post:
          url: "/deposit"
          headers:
            koreUserId: "bhhavvya"
            botId: "invalidBotId"
            accountId: "invalidAccountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
        # When the botId and accountId is invalid, the server should respond with a 401 status code and an error message.
      - think: 3

      - log: "Sending an invalid request with incorrect authorization token"
        post:
          url: "/deposit"
          headers:
            koreUserId: "bhhavvya"
            botId: "validBotId"
            accountId: "validAccountId"
            Authorization: "invalidAuthToken"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
        # When the Authorization token is invalid, the server should respond with a 401 status code and an error message.
      - think: 3

      - log: "Sending a request to an invalid URL"
        post:
          url: "/invalidDepositURL"
          headers:
            koreUserId: "bhhavvya"
            botId: "validBotId"
            accountId: "validAccountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
        # When the URL is invalid, the server should respond with a 404 status code and an error message.
      - think: 3
