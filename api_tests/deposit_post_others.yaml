# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=70db6d5eac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "deposit_post_others.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Simulating failed authentication"
        post:
          url: "/deposit"
          headers:
            koreUserId: "invalid"
            botId: "invalid"
            accountId: "invalid"
            Authorization: "invalid"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1

      - log: "Simulating invalid payloads"
        post:
          url: "/deposit"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "{{ $processEnvironment.AUTH_TOKEN }}"
            accountId: "{{ $processEnvironment.AUTH_TOKEN }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "invalid"
            sourceAccountId: "invalid"
            amount: "invalid"
            currency: "invalid"
            image:
              frontImage: "invalid"
              backImage: "invalid"
          expect:
            - statusCode: 422
            - hasProperty: "err.message"
      - think: 1

      - log: "Simulating incorrect URL"
        get:
          url: "/wrong_url"
          headers:
            koreUserId: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "{{ $processEnvironment.AUTH_TOKEN }}"
            accountId: "{{ $processEnvironment.AUTH_TOKEN }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1
