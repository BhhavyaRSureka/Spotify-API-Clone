# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /DeleteRecurringTransfers_post for http method type POST in artillery framework

# RoostTestHash=2abad00381


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "DeleteRecurringTransfers_post_success.csv"
    fields: ["koreUserId", "botId", "userCode", "accountId", "paymentId", "updateTransferType", "customerId"]
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
scenarios:
  - name: "Delete Recurring Transfers"
    flow:
      - log: "Initiating request to delete recurring transfers"
      - post:
          url: "/DeleteRecurringTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "content"
      - think: 5
