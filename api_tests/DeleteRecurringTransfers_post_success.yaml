# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /DeleteRecurringTransfers_post for http method type POST in artillery framework

# RoostTestHash=2abad00381


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "DeleteRecurringTransfers_post_success.csv"
    fields:
      - "customerId"
      - "paymentId"
      - "updateTransferType"
    skipHeader: true
  environments:
    dev:
      target: "http://dev.example.com"

scenarios:
  - name: "Delete Recurring Transfers"
    flow:
      - log: "Starting test for deleting recurring transfers"
      - post:
          url: "/DeleteRecurringTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: ["message", "Recurring transfer deleted successfully"]
      - think: 1
      - log: "End of test for deleting recurring transfers"
