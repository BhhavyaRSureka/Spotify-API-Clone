# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=207beafabe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetTransactions_post_others.csv"
    fields:
      - "customerId"
      - "accountNumber"
      - "category"
      - "startDate"
      - "endDate"
      - "status"
      - "transactionAmount"
    skipHeader: true

scenarios:
  - name: "API Failure Scenarios"
    flow:
      - log: "POST request with invalid payload"
        post:
          url: "/GetTransactions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "INVALID"
            botId: "INVALID"
            accountId: "INVALID"
            X-Auth-token: "INVALID"
            X-CORRELATION-ID: 123456789
          json:
            customerId: "{{ customerId }}"
            accountNumber: "{{ accountNumber }}"
            category: "{{ category }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 400
            - hasProperty: "errorMessage"
        think: 1

      - log: "POST request with invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "INVALID"
            botId: "INVALID"
            accountId: "INVALID"
            X-Auth-token: "INVALID"
            X-CORRELATION-ID: 123456789
          json:
            customerId: "{{ customerId }}"
            accountNumber: "{{ accountNumber }}"
            category: "{{ category }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 404
            - hasProperty: "errorMessage"
        think: 1

      - log: "POST request with invalid Authorization token"
        post:
          url: "/GetTransactions"
          headers:
            Authorization: "INVALID_TOKEN"
            koreUserId: "INVALID"
            botId: "INVALID"
            accountId: "INVALID"
            X-Auth-token: "INVALID"
            X-CORRELATION-ID: 123456789
          json:
            customerId: "{{ customerId }}"
            accountNumber: "{{ accountNumber }}"
            category: "{{ category }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 401
            - hasProperty: "errorMessage"
        think: 1
