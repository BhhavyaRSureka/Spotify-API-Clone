# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=207beafabe

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetTransactions_post_others.csv"
    fields:
      - "customerId"
      - "accountNumber"
      - "category"
      - "startDate"
      - "endDate"
      - "status"
      - "transactionAmount"
    skipHeader: true

scenarios:
  - name: "Invalid API endpoint test"
    flow:
      - log: "Testing with invalid API endpoint"
      - post:
          url: "/InvalidGetTransactions"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "InvalidUser"
            botId: "InvalidBot"
            accountId: "InvalidAccount"
            X-Auth-token: "InvalidToken"
            X-CORRELATION-ID: 999999
          json:
            customerId: "{{ customerId }}"
            accountNumber: "{{ accountNumber }}"
            category: "{{ category }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 404
            - hasProperty: 'error'
      - think: 1

  - name: "Invalid request body test"
    flow:
      - log: "Testing with invalid request body"
      - post:
          url: "/GetTransactions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "TestUser"
            botId: "TestBot"
            accountId: "TestAccount"
            X-Auth-token: "TestToken"
            X-CORRELATION-ID: 123456
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
      - think: 1

  - name: "Missing required fields in request body test"
    flow:
      - log: "Testing with missing required fields in request body"
      - post:
          url: "/GetTransactions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "TestUser"
            botId: "TestBot"
            accountId: "TestAccount"
            X-Auth-token: "TestToken"
            X-CORRELATION-ID: 123456
          json:
            customerId: "{{ customerId }}"
            accountNumber: []
            category: "{{ category }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
      - think: 1

  - name: "Unauthorized access test"
    flow:
      - log: "Testing with unauthorized access"
      - post:
          url: "/GetTransactions"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "TestUser"
            botId: "TestBot"
            accountId: "TestAccount"
            X-Auth-token: "TestToken"
            X-CORRELATION-ID: 123456
          json:
            customerId: "{{ customerId }}"
            accountNumber: "{{ accountNumber }}"
            category: "{{ category }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
      - think: 1
