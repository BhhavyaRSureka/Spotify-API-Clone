# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments_post for http method type POST in artillery framework
# RoostTestHash=4c492efad0



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "shipments_post_others.csv"
    fields:
      - "x-customer-transaction-id"
      - "content-type"
      - "x-locale"
      - "authorization"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Non-successful API requests"
    flow:
      # Invalid payload
      - post:
          url: "/ship/v1/shipments"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400
        think: 1

      # Missing authorization
      - post:
          url: "/ship/v1/shipments"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
          json:
            validKey: "validValue"
          expect:
            - statusCode: 401
        think: 1

      # Invalid URL
      - post:
          url: "/ship/v1/invalid"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            validKey: "validValue"
          expect:
            - statusCode: 404
        think: 1
