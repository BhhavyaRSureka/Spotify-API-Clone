# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2abad00381

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "DeleteRecurringTransfers_post_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "userCode"
      - "accountId"
      - "customerId"
      - "paymentId"
      - "updateTransferType"
    skipHeader: true

scenarios:
  - name: "Scenario 1 - Testing failure and edge cases for /DeleteRecurringTransfers API"
    flow:
      - think: 5
      - post:
          url: "/DeleteRecurringTransfers"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
          log: "Test case for Invalid authorization, expecting 401 status code and error message"
      - think: 5
      - post:
          url: "/DeleteRecurringTransfers"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "{{ koreUserId }}"
            botId: ""
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
          log: "Test case for missing required header 'botId', expecting 404 status code and error message"
      - think: 5
      - post:
          url: "/DeleteRecurringTransfersInvalidUrl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
          json:
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
          log: "Test case for invalid URL, expecting 404 status code and error message"
