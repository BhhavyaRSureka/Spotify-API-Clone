# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=2abad00381

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "DeleteRecurringTransfers_post_others.csv"
    fields:
      - "customerId"
      - "paymentId"
    skipHeader: true

scenarios:
  - name: "API Failure Scenarios"
    flow:
      - log: "Testing with incorrect URL"
        post:
          url: "/DeleteRecurringTransfer" # incorrect URL
          headers:
            koreUserId: "bhhavvya"
            botId: "bot123"
            userCode: "user123"
            accountId: "acc123"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            updateTransferType: "all"
          expect:
            - statusCode: 404 # Expecting Not Found Error

      - think: 1

      - log: "Testing with invalid authorization token"
        post:
          url: "/DeleteRecurringTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "bot123"
            userCode: "user123"
            accountId: "acc123"
            Authorization: "InvalidToken" # Invalid Authorization Token
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            updateTransferType: "all"
          expect:
            - statusCode: 401 # Expecting Authorization Error

      - think: 1

      - log: "Testing with missing required headers"
        post:
          url: "/DeleteRecurringTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "" # Missing botId
            userCode: "user123"
            accountId: "" # Missing accountId
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            updateTransferType: "all"
          expect:
            - statusCode: 400 # Expecting Bad Request Error
