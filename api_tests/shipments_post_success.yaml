# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments_post for http method type POST in artillery framework
# RoostTestHash=4c492efad0



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "shipments_post_success.csv"
    fields:
      - "x-customer-transaction-id"
      - "content-type"
      - "x-locale"
      - "authorization"
    skipHeader: true

scenarios:
  - name: "Test successful API responses"
    flow:
      - post:
          url: "/ship/v1/shipments"
          headers:
            x-customer-transaction-id: "{{ x-customer-transaction-id }}"
            content-type: "{{ content-type }}"
            x-locale: "{{ x-locale }}"
            authorization: "{{ authorization }}"
          expect:
            - statusCode: 200
            - hasProperty: "transactionId"
            - hasProperty: "customerTransactionId"
            - hasProperty: "output"
            - hasProperty: "output.transactionShipments"
            - hasProperty: "output.alerts"
            - hasProperty: "output.jobId"
          think: 1
