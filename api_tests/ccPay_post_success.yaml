# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /ccPay_post for http method type POST in artillery framework

# RoostTestHash=c6fa681866


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "ccPay_post_success.csv"
    fields:
      - "amount"
      - "currency"
      - "customerId"
      - "dueDate"
      - "sourceAccountId"
      - "targetAccountId"
      - "transactionType"
    skipHeader: true
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        authToken: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "CCPay successful API requests"
    flow:
      - post:
          url: "/ccPay"
          headers:
            koreUserId: "bhhavvya"
            botId: "botId"
            accountId: "accountId"
            Authorization: "{{ authToken }}"
          json:
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            customerId: "{{ customerId }}"
            dueDate: "{{ dueDate }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            transactionType: "{{ transactionType }}"
          expect:
            - statusCode: 200
            # Add more assertions as per the response schema
          capture:
            json: "$.message"
            as: "responseMessage"
        log: "CCPay API request with valid payload was successful"
      - think: 1
