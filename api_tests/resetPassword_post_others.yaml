# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=79c3f016d8

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "resetPassword_post_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-success API responses testing"
    flow:
      - log: "Send invalid payload"
        post:
          url: "/resetPassword"
          json:
            username: "InvalidUsername"
            password: "1111"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: ["err.message"]
        think: 1

      - log: "Send request without required fields"
        post:
          url: "/resetPassword"
          json:
            username: ""
            password: ""
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: ["err.message"]
        think: 1

      - log: "Send request with incorrect URL"
        post:
          url: "/incorrectUrl"
          json:
            username: "John"
            password: "1111"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: ["err.message"]
        think: 1

      - log: "Send request with invalid authorization token"
        post:
          url: "/resetPassword"
          json:
            username: "John"
            password: "1111"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: ["err.message"]
        think: 1
