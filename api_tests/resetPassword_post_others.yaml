# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=79c3f016d8

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./custom-processor.js"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "resetPassword_post_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Test reset password endpoint for failure cases"
    flow:
      - log: "Sending request with missing username field"
        post:
          url: "/resetPassword"
          auth:
            header: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            password: "1111"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
      - log: "Sending request with missing password field"
        post:
          url: "/resetPassword"
          auth:
            header: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "John"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
      - log: "Sending request with invalid URL"
        post:
          url: "/resetPasswordInvalid"
          auth:
            header: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "John"
            password: "1111"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1
      - log: "Sending request with invalid authorization token"
        post:
          url: "/resetPassword"
          auth:
            header: "InvalidToken"
          json:
            username: "John"
            password: "1111"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
