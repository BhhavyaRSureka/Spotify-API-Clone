# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# Test generated for /sendsecuremessage_post for http method type POST in artillery framework
# RoostTestHash=3b0ac86130

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "sendsecuremessage_post_success.csv"
    fields: 
      - "authType"
      - "firstName"
      - "lastName"
      - "email"
      - "phoneNumber"
      - "topic"
      - "message"
      - "availableTime"
      - "chatHistory"
      - "cif"
    skipHeader: true
  processor: "./my_processor.js"
  plugins:
    expect: {}

scenarios:
  - name: "SecureMessage Scenario"
    flow:
      - log: "Sending a secure message"
      - post:
          url: "/sendsecuremessage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            authType: "{{ authType }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            phoneNumber: "{{ phoneNumber }}"
            topic: "{{ topic }}"
            message: "{{ message }}"
            availableTime: "{{ availableTime }}"
            chatHistory: "{{ chatHistory }}"
            cif: "{{ cif }}"
          expect:
            - statusCode: 200
            - headers:
                "content-type": "application/json"
      - think: 1
