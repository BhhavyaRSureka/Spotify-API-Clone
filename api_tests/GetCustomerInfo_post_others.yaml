# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=88810bf424

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetCustomerInfo_post_others.csv"
    fields:
      - "botId"
      - "accountId"
      - "customerId"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10
  variables:
    koreUserId: "bhhavvya"
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "API Failure Test Scenario"
    flow:
      - log: "Test Case: Invalid Authorization Token"
        post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
      - log: "Test Case: Invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ authToken }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
      - think: 1
      - log: "Test Case: Missing Required Headers"
        post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
      - think: 1
      - log: "Test Case: Invalid CustomerId"
        post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ authToken }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "InvalidCustomerId"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
