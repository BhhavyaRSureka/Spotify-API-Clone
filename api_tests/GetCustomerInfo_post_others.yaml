# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=88810bf424

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetCustomerInfo_post_others.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "Authorization"
      - "X-Auth-token"
      - "X-CORRELATION-ID"
    skipHeader: true
scenarios:
  - name: "Get customer info test scenario"
    flow:
      - log: "Test with invalid URL"
        post:
          url: "/GetCustomerInfo_invalid"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ Authorization }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
        think: 1
      - log: "Test with invalid authorization token"
        post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "Invalid token"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
        think: 1
      - log: "Test with missing required headers"
        post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: ""
            accountId: "{{ accountId }}"
            Authorization: "{{ Authorization }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: 1045300
          expect:
            - statusCode: 400
        think: 1
      - log: "Test with missing required request body"
        post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ Authorization }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json: {}
          expect:
            - statusCode: 400
        think: 1
