# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetTransactions_post for http method type POST in artillery framework

# RoostTestHash=207beafabe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "GetTransactions_post_success.csv"
    fields:
      - "customerId"
      - "accountNumber"
      - "category"
      - "startDate"
      - "endDate"
      - "status"
      - "transactionAmount"
    skipHeader: true
  environments:
    development:
      target: "http://localhost:3000"
    production:
      target: "{{ $processEnvironment.API_HOST }}"
scenarios:
  - name: "GetTransactions test"
    flow:
      - log: "Starting GetTransactions test"
      - post:
          url: "/GetTransactions"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
            accountNumber: "{{ accountNumber }}"
            category: "{{ category }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "transactionId"
            - hasProperty: "accountNumber"
            - hasProperty: "timestamp"
            - hasProperty: "amount"
            - hasProperty: "currency"
            - hasProperty: "debitOrCredit"
            - hasProperty: "description"
            - hasProperty: "paymentMode"
            - hasProperty: "DBANames"
            - hasProperty: "status"
            - hasProperty: "transactionType"
            - hasProperty: "MCCCode"
            - hasProperty: "name"
            - hasProperty: "address"
            - hasProperty: "street"
            - hasProperty: "city"
            - hasProperty: "state"
            - hasProperty: "zip"
            - hasProperty: "country"
            - hasProperty: "phoneNumber"
            - hasProperty: "CCTier2Data"
            - hasProperty: "receiverFirstName"
            - hasProperty: "receiverLastName"
            - hasProperty: "receiverBank"
            - hasProperty: "receiverCountry"
            - hasProperty: "senderFirstName"
            - hasProperty: "senderLastName"
            - hasProperty: "senderBank"
            - hasProperty: "senderCountry"
            - hasProperty: "id"
      - think: 1
      - log: "Finished GetTransactions test"
