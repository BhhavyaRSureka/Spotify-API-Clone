# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetTransactions_post for http method type POST in artillery framework

# RoostTestHash=207beafabe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetTransactions_post_success.csv"
    fields:
      - "koreUserId"
      - "botId"
      - "accountId"
      - "Authorization"
      - "X-Auth-token"
      - "X-CORRELATION-ID"
    skipHeader: true

scenarios:
  - name: "Test GetTransactions API"
    flow:
      - post:
          url: "/GetTransactions"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "1045300"
            accountNumber: ["1045301"]
            category:
              - codeType: "MCC"
                value: [[5811,5814]]
                source: "Internal"
            startDate: "2021-04-01T00:00:00.000+0000"
            endDate: "2021-04-30T00:00:00.000+0000"
            status: "Posted"
            transactionAmount:
              amount:
                gt: 1000
              currency: "USD"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "transactionId"
            - hasProperty: "accountNumber"
            - hasProperty: "timestamp"
            - hasProperty: "amount"
            - hasProperty: "currency"
            - hasProperty: "debitOrCredit"
            - hasProperty: "description"
            - hasProperty: "paymentMode"
            - hasProperty: "DBANames"
            - hasProperty: "status"
            - hasProperty: "transactionType"
            - hasProperty: "MCCCode"
            - hasProperty: "name"
            - hasProperty: "address"
            - hasProperty: "street"
            - hasProperty: "city"
            - hasProperty: "state"
            - hasProperty: "zip"
            - hasProperty: "country"
            - hasProperty: "phoneNumber"
          log: "GetTransactions request sent successfully with status code 200"
      - think: 1
