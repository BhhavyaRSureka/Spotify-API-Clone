# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# RoostTestHash=7b4d2d87f9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetScheduleTransfers_post_others.csv"
    fields: ["koreUserId", "botId", "userCode", "accountId", "customerId", "status"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test scenario for GetScheduleTransfers API"
    flow:
      - log: "Test case: Authorization failure"
        post:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "InvalidToken"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1
      - log: "Test case: Invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1
      - log: "Test case: Missing required header field"
        post:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: ""
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "err"
            - hasProperty: "err.message"
      - think: 1
