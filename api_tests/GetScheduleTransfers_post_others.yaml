# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=7b4d2d87f9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "GetScheduleTransfers_post_others.csv"
    fields:
      - "customerId"
      - "status"
  plugins:
    expect: {}
scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending an invalid request with missing required headers"
        post:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "bhhavvya"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
            - hasProperty: "err.message"
            - equals: ["err.message", "Error: Not Found"]
      - think: 1
      - log: "Sending a request with invalid authorization token"
        post:
          url: "/GetScheduleTransfers"
          headers:
            koreUserId: "bhhavvya"
            botId: "invalid_bot_id"
            userCode: "invalid_user_code"
            accountId: "invalid_account_id"
            Authorization: "invalid_auth_token"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
            - hasProperty: "err.message"
            - equals: ["err.message", "Error: Authorisation failed."]
      - think: 1
      - log: "Sending a request to an invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            koreUserId: "bhhavvya"
            botId: "invalid_bot_id"
            userCode: "invalid_user_code"
            accountId: "invalid_account_id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
