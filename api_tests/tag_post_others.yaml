# ********RoostGPT********
# Test generated by RoostGPT for test Artillery-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /ship/v1/shipments/tag_post for http method type POST in artillery framework
# RoostTestHash=cc7400f160



# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "tag_post_others.csv"
    fields:
      - "transaction_id"
      - "locale"
    skipHeader: true

scenarios:
  - name: "Non-Success API Requests Scenario"
    flow:
      # Invalid URL case
      - post:
          url: "/ship/v1/shipments/invalid_tag"
          headers:
            x-customer-transaction-id: "{{ transaction_id }}"
            content-type: "application/json"
            x-locale: "{{ locale }}"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tag: "invalidTag"
          expect:
            - statusCode: 404 # Expecting Not Found error
          think: 1

      # Invalid payload case
      - post:
          url: "/ship/v1/shipments/tag"
          headers:
            x-customer-transaction-id: "{{ transaction_id }}"
            content-type: "application/json"
            x-locale: "{{ locale }}"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidKey: "invalidValue"
          expect:
            - statusCode: 400 # Expecting Bad Request error
          think: 1

      # Missing authorization case
      - post:
          url: "/ship/v1/shipments/tag"
          headers:
            x-customer-transaction-id: "{{ transaction_id }}"
            content-type: "application/json"
            x-locale: "{{ locale }}"
          json:
            tag: "validTag"
          expect:
            - statusCode: 401 # Expecting Unauthorized error
          think: 1
