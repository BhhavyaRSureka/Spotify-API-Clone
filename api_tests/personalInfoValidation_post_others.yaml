# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# RoostTestHash=be461d4c4c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "personalInfoValidation_post_others.csv"
    fields:
      - "customerId"
      - "dob"
      - "ssn"
      - "secrectPassword"
      - "AccNumber"
      - "lastDepositAmt"
    skipHeader: true
  environments:
    staging:
      target: "http://api.staging.example.com"
      http:
        pool: 10
scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending invalid request with missing required fields"
        post:
          url: "/personalInfoValidation"
          headers:
            Authorization: "Bearer invalid_token"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
            - hasProperty: ["err.message"]
      - think: 5
      - log: "Sending request to non-existent URL"
        get:
          url: "/nonExistentUrl"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: ["err.message"]
      - think: 5
      - log: "Sending request with invalid payload"
        post:
          url: "/personalInfoValidation"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            dob: "invalid_date"
            ssn: "invalid_ssn"
            secrectPassword: "{{ secrectPassword }}"
            AccNumber: "{{ AccNumber }}"
            lastDepositAmt: "{{ lastDepositAmt }}"
          expect:
            - statusCode: 400
            - hasProperty: ["err.message"]
      - think: 5
