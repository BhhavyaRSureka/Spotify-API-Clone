# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4

# Test generated for /GetCCSchedule_post for http method type POST in artillery framework
# RoostTestHash=b38ec5fe10

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetCCSchedule_post_success.csv"
    fields:
      - "customerId"
      - "status"
  plugins:
    expect: {}
    
scenarios:
  - name: "GetCCSchedule API test"
    flow:
      - log: "Starting test for GetCCSchedule API"
      - post:
          url: "/GetCCSchedule"
          headers:
            koreUserId: "sampleUserId"
            botId: "sampleBotId"
            accountId: "sampleAccountId"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "DueDate"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "id"
            - hasHeader: "content-type"
      - think: 1
      - log: "Test for GetCCSchedule API completed successfully"
