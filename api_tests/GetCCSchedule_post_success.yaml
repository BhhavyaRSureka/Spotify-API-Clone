# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetCCSchedule_post for http method type POST in artillery framework

# RoostTestHash=b38ec5fe10


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetCCSchedule_post_success.csv"
    fields:
      - "customerId"
      - "status"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "GetCCSchedule API Test"
    flow:
      - log: "Starting GetCCSchedule API Test"
      - post:
          url: "/GetCCSchedule"
          headers:
            koreUserId: "bhhavvya"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "DueDate"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "id"
          capture:
            json: "$.Type"
            as: "Type"
          log: "Posted GetCCSchedule request"
      - think: 1
      - log: "Finished GetCCSchedule API Test"
