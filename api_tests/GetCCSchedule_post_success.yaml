# Test generated by RoostGPT for test Testing12345 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetCCSchedule_post for http method type POST in artillery framework

# RoostTestHash=b38ec5fe10


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "GetCCSchedule_post_success.csv"
    fields:
      - "customerId"
      - "status"
  plugins:
    expect: {}
scenarios:
  - name: "Test GetCCSchedule API"
    flow:
      - log: "Sending GET request to /GetCCSchedule"
      - get:
          url: "/GetCCSchedule"
          headers:
            koreUserId: "test_user"
            botId: "test_bot"
            accountId: "test_account"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            # Assert response header
            - hasProperty: headers
            # Assert response body
            - hasProperty: body
            - hasProperty: "body[0].Type"
            - hasProperty: "body[0].CustomerId"
            - hasProperty: "body[0].Amount"
            - hasProperty: "body[0].SourceAccountNumber"
            - hasProperty: "body[0].TargetAccountNumber"
            - hasProperty: "body[0].DueDate"
            - hasProperty: "body[0].Currency"
            - hasProperty: "body[0].Status"
            - hasProperty: "body[0].id"
      - think: 1
